export enum Operator {
  is_null,
  not_null,
  str_eq,
  n_str_eq,
  str_in,
  n_str_in,
  regex,
  num_eq,
  n_num_eq,
  gt,
  lt,
  gte,
  lte,
  num_between,
  date_eq,
  n_date_eq,
  before,
  after,
  date_between,
}
